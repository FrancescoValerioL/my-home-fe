export default interface Book {
    id: number,
    title: string,
    author: string,
    comic: boolean,
    volumes: number,
    pages: number,
    year: number,
    desc: string,
    read: boolean,
    packed: boolean,
}